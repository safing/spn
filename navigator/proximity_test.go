package navigator

/*
import "testing"

func TestProximityCollection(t *testing.T) {
	col := NewProximityCollection(10)

	for i := 0; i <= 100; i++ {
		col.Add(&ProximityResult{Proximity: i})
	}
	col.Clean()

	if len(col.All) != 10 {
		t.Errorf("len(col.All) != 10, is %d", len(col.All))
	}
	if cap(col.All) != 20 {
		t.Errorf("cap(col.All) != 20, is %d", cap(col.All))
	}
	if col.MinProximity != 90 {
		t.Errorf("col.MinProximity != 90, is %d", col.MinProximity)
	}

}
*/
