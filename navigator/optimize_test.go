package navigator

// TODO: fix
/*
import (
	"sync"
	"testing"
)

func TestOptimize(t *testing.T) {

	collection := buildTestNet()
	var lock sync.Mutex

	new, err := optimizeNetwork("1", collection, &lock)
	if err != nil {
		t.Fatal(err)
	}

	t.Logf("furthest node: %s", new)
	if new.PortName != "10" && new.PortName != "12" {
		t.Fatal("wrong result")
	}

}
*/
